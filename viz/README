This allows visualization of the triangles that result from the 
earcut algorithm

## To use

$ cargo test -- --test-threads=1

The test-threads=1 is required for it to work properly. 
This test process should generate viz/testoutput.js

Now open the html file with a circa 2018 web browser, for example:

$ firefox viz.html

## Adding new data

To add new data, create a new json data file under ../tests/fixtures
Re-run the tests, then reload viz.html
