1. this allows visualization of the triangles that result from the 
   earcut algorithm

2. to use it, open 'viz.html' in a circa-2018 web browser

3. to add new data, create a new json data file under ../tests/fixtures
   then run 'makedata.sh' inside this directory on a macOS/linux/bsd system
